package de.teamholy.api.bukkit.listeners;

import org.bukkit.event.Listener;

/* copyright by Yassino */
public class AntiCrashListener implements Listener {

/*    private final HashSet<UUID> teleported = new HashSet<>();


    @EventHandler
    public void onMove(PlayerMoveEvent event) {
        if (teleported.remove(event.getPlayer().getUniqueId())) {
            return;
        }

        if (event.getFrom().distance(event.getTo()) >= BukkitHolyAPI.getInstance().getMaxMoveDistance()) {
            event.getPlayer().kickPlayer("Crash lol");
        }

    }

    @EventHandler
    public void onTeleport(PlayerTeleportEvent event) {
        teleported.add(event.getPlayer().getUniqueId());
    }*/

}
